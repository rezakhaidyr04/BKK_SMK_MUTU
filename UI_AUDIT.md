# UI Audit — BKK SMK MUTU

Ringkasan cepat (otomatis):
- Area dipindai: `resources/views/**` dan `resources/views/components/ui/**`
- Temuan utama: campuran penggunaan komponen UI (`x-ui.*`) dan banyak inline styles serta variasi kelas CSS (badge, nav-badge, ui-badge, ui-btn, inline `style=`). Ada juga placeholder links seperti `href="#"` di beberapa view.

Masalah yang terlihat:
- Konsistensi: Beberapa halaman memakai `x-ui.*` components (good), namun banyak file masih menggunakan inline styles atau bespoke markup (cards, hero, stat cards). Ini membuat tampilan tidak konsisten.
- Reusability: Repetisi markup (panel, stat cards, badges, buttons) yang seharusnya menjadi komponen.
- Accessibility: Beberapa emoji/presentational SVG tidak diberi `aria-hidden` atau teks alternatif konsisten.
- Placeholder links: `href="#"` ditemukan beberapa lokasi — sudah diperbaiki sebagian, masih ada beberapa.

Contoh file dengan masalah yang menonjol:
- `resources/views/admin/reports/index.blade.php` — banyak `style="..."` inline, gradient, cards.
- `resources/views/company/profile/edit.blade.php` — inline styles for layout/preview.
- `resources/views/components/ui/company-dashboard.blade.php` — mix of inline styles with `x-ui.btn` usage.
- `resources/views/welcome.blade.php` — hero uses many inline styles.

Counts (otomatis):
- Files with `style="` inline CSS: > 40 (lihat hasil grep di run saya).
- Files using `ui-badge` / `status-badge`: ~30 locations.
- Files using `ui-btn` / `x-ui.btn`: ~50 locations.

Recommended approach (phased):
1. Establish core components & tokens (small, safe):
   - `x-ui.btn` (variants: primary, secondary, ghost, company)
   - `x-ui.status-badge` (already exists) — ensure all badges use it
   - `x-ui.panel`, `x-ui.stat-card`, `x-ui.page-hero` (already exist — ensure reuse)
   - Tailwind tokens in `tailwind.config.js` (colors, radii, spacing)
2. Replace inline badges and nav badges with `x-ui.status-badge` (low risk).
3. Replace isolated inline `style=` blocks for small elements (badges, pills, avatars) with classes or components.
4. Larger hero/panel refactors: extract to component `x-ui.hero` and migrate markup progressively.
5. Accessibility pass: ensure presentational icons are `aria-hidden` and interactive elements have labels.

Suggested immediate automated work (optional, can run now if you OK):
- Replace occurrences of `<span class="ui-badge ...">` with `<x-ui.status-badge status="...">` where pattern matches.
- Replace `href="#"` with `javascript:void(0);` + `role="button" aria-disabled="true"` or map to real routes when available.
- Add `aria-hidden="true"` to presentational emojis/SVGs where needed.

Risiko & notes:
- Wide automated replacements can change visual details; run on a feature branch and visually test key pages (home, company dashboard, admin companies, job listing, application pages).
- Keep a migration checklist and visual screenshots before/after for QA.

Next actions I can take for you (pick one):
- A. Run a safe automated pass: replace `ui-badge` spans → `x-ui.status-badge` and fix leftover `href="#"` (recommended first step).
- B. Start extracting `x-ui.hero` by refactoring `welcome.blade.php` into component (bigger risk, do after A).
- C. Generate a PR with `UI_AUDIT.md` and a branch containing automated changes in A.
- D. Do nothing now — you review the audit and tell me which steps to take.

Tell me which option you want (A/B/C/D) and I'll proceed. If you pick A or C, confirm I should commit & push changes to the current branch `landing/finalize-20260720140424`.

— Generated by audit script (assistant). Review before merging.
